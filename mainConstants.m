function [modelType,changeRetModel,selectNewData,counterModel,compound,counterData,bestParam,optimMethod,optimData,solutes,removeData]=mainConstants()
  modelType=0;
  counterModel=0;
  counterData=0;
  compound="";
  changeRetModel=true;
  selectNewData=true;
  bestParam=100;
  optimMethod=0;
  optimData=zeros(0,0,0);
  solutes=zeros(0,0);
  removeData=true;
  end
